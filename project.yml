name: TokenUsageMonitor
options:
  bundleIdPrefix: com.example
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: NO
    APP_BUNDLE_ID: com.example.tokenusagemonitor
    WIDGET_BUNDLE_ID: com.example.tokenusagemonitor.widget
    APP_GROUP_IDENTIFIER: group.example.tokenusagemonitor

targets:
  TokenUsageMonitor:
    type: application
    platform: macOS
    sources:
      - App
      - Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(APP_BUNDLE_ID)
        PRODUCT_NAME: TokenUsageMonitor
        PROVISIONING_PROFILE_SPECIFIER: $(APP_PROVISIONING_PROFILE_SPECIFIER)
    info:
      path: App/Info.plist
      properties:
        LSApplicationCategoryType: public.app-category.developer-tools
        NSHumanReadableCopyright: ""
    entitlements:
      path: App/TokenUsageMonitor.entitlements
      properties:
        com.apple.security.application-groups:
          - $(APP_GROUP_IDENTIFIER)
    dependencies:
      - target: TokenUsageMonitorExtension

  TokenUsageMonitorExtension:
    type: app-extension
    platform: macOS
    sources:
      - Widget
      - Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(WIDGET_BUNDLE_ID)
        PRODUCT_NAME: TokenUsageMonitorExtension
        SKIP_INSTALL: YES
        PROVISIONING_PROFILE_SPECIFIER: $(WIDGET_PROVISIONING_PROFILE_SPECIFIER)
    info:
      path: Widget/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    entitlements:
      path: Widget/TokenUsageMonitorExtension.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.application-groups:
          - $(APP_GROUP_IDENTIFIER)

  TokenUsageMonitorTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
      - Shared
      - App/Services
      - App/Providers
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.tokenusagemonitor.tests
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_IDENTITY: "-"

schemes:
  TokenUsageMonitor:
    build:
      targets:
        TokenUsageMonitor: all
        TokenUsageMonitorTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - TokenUsageMonitorTests
