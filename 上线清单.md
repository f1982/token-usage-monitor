# Token Usage Monitor 上线清单

本清单覆盖两个发布渠道：Mac App Store 和 GitHub。两种构建应尽量共享同一套沙盒、文件授权和隐私行为。

## 已完成

- [x] GitHub 发布流程已实现并测试通过：Developer ID 签名、notarization、打包和 GitHub Release。

## 当前迭代：文件访问与沙盒

- [x] 移除直接扫描用户任意路径的设计。
- [x] 使用 `NSOpenPanel` 让用户主动选择 Claude Projects、Codex Sessions 和 statusline 目录。
- [x] 保存并恢复 security-scoped bookmarks。
- [x] 在每次扫描前后正确调用 `startAccessingSecurityScopedResource()` 和 `stopAccessingSecurityScopedResource()`。
- [x] 没有授权时显示清晰提示，不能静默读取默认路径。
- [x] 增加重新授权和撤销目录访问的入口。
- [x] 启用主应用 App Sandbox。
- [x] 补齐沙盒所需的网络客户端 entitlement。
- [ ] 使用真实的 Team ID、Bundle ID 和已注册 App Group。
- [ ] 验证主应用与 Widget 在沙盒下共享缓存正常。

## OAuth 与数据来源

- [x] 决定是否从 Mac App Store 版本移除本地 OAuth token 读取。
- [x] 如果保留 OAuth，改为用户主动授权/输入，并通过 Keychain 保存。
- [x] 明确说明 OAuth 请求会向 Anthropic 发送哪些数据。
- [x] 检查 Claude Code、Codex 名称和接口使用是否需要额外授权或免责声明。

## 隐私与应用内体验

- [x] 添加公开 Privacy Policy URL。
- [x] 在应用内提供 Privacy Policy 和 Support 链接。
- [x] 提供清除缓存和撤销文件访问授权的功能。
- [x] 说明项目名称、路径、模型名、token 总数和时间戳的本地缓存策略。
- [ ] 在 App Store Connect 填写准确的 App Privacy 信息。
- [ ] 评估并加入适用的 `PrivacyInfo.xcprivacy`。

## Mac App Store 发布

- [x] 增加 Mac App Store 专用签名和 provisioning profile 流程。
- [x] 确保 App Store 构建不包含 GitHub 自动更新组件。
- [ ] 在干净 macOS 用户环境测试首次目录授权、重启后 bookmark 和 Widget。
- [ ] 使用 Transporter 或 Xcode 上传并通过 App Store Connect 验证。
- [x] 准备审核说明：数据来源、目录授权、OAuth 开关和测试步骤。

## GitHub 发布维护

- [x] Developer ID 签名和 notarization 已实现并测试通过。
- [ ] 如果加入 Sparkle，只在 GitHub 构建中启用。
- [x] 生成发布包校验和并随 GitHub Release 发布。
- [x] 验证 GitHub 构建升级时不会覆盖或误识别 Mac App Store 构建。

## 发布前验证

- [x] 全量单元测试通过：131 个测试，0 failures（通过 `build-for-testing` + `xcrun xctest` 验证）。
- [ ] 验证沙盒 entitlement、签名和 App Group entitlement。
- [ ] 验证没有授权时无法读取用户目录。
- [ ] 验证用户撤销或移动目录后应用不会崩溃。
- [ ] 验证网络请求、缓存和 Widget 数据符合隐私政策。
- [ ] 在最新支持的 macOS 版本上完成 Mac App Store 和 GitHub 两种构建的冒烟测试。
